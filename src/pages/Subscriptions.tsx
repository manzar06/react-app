import { Typography, Box } from '@mui/material';

const Subscriptions = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Subscriptions
      </Typography>
      <Typography>
        This page will display the user's subscription preferences and allow them to manage their subscriptions.
      </Typography>
    </Box>
  );
};

export default Subscriptions; 
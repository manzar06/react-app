import { Typography, Box } from '@mui/material';

const Topics = () => {
  return (
    <Box>
      <Typography variant="h4" gutterBottom>
        Topics
      </Typography>
      <Typography>
        This page will display a list of available topics and allow users to filter posts by topic.
      </Typography>
    </Box>
  );
};

export default Topics; 